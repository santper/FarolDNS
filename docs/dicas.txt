04/06/2026 - Minhas dicas para falar com a IA agy:

Caso a placa FarolDNS perca a ethernet, o Wi-Fi pode assumir o mesmo IP da eth de modo que ficaria transparente para o roteador, só mais lento um pouco. Seria mais interessante ainda se a Wi-Fi conseguir ficar com os dois IPs, o IP dela e o IP da eth, se isso for possível com o ESP32-S3. Caso a eth volte o IP pula de volta para ela.

Outra possibilidade é só funcionar uma interface por vez, com apenas um IP na placa. Se conectar cabo de rede e a ethernet subir então o Wi-Fi fica desativado. Se não houver W5500, ou se não conectar o cabo de rede, ou se não conseguir se comunicar pelo cabo, então ativa o Wi-Fi. Acho essa a opção mais racional pois só é necessário um canal de comunicação por vez para o funcionamento do DNS Server, facilita a configuração do roteador Huawei que só aceita dois DNS Server's (serão dois dispositivos FarolDNS para redundância de equipamento) e ainda por cima economiza energia e banda de Wi-Fi.


19/06/2026 - Minhas dicas para falar com a IA:
Atualize logo o historico_projeto.md e GitHub antes de acabar os créditos.

Sempre usei a máxima que servidor deve ter IP estático. Inclusive reservo no DHCP do roteador a faixa de .1 a .19 para servidores e equipamentos de rede.
Vamos aprimorar a lógica:
1. Na UI web deve poder optar por habilitar o Wi-Fi, a Ethernet ou os dois;
2. Se os dois estiverem habilitados e com o mesmo IP, o software só usa um dos dois, com prioridade para a Ethernet (como está agora);
3. Se configurar um IP para cada, os dois ficam ativos ao mesmo tempo, inclusive, se você disser que é possível, em sub-redes diferentes. Deverá ter um nome mDNS para cada interface.

A UI web não está mostrando o IP adquirido. Sendo IP estático não será mais problema. Mas deveria dar alguma informação de atividade do dispositivo, qual interface está ativa, bytes transmitidos/recebidos, lista de url no cache, sei lá, alguma informação útil que indique que o dispositivo está vivo e atuante.

 